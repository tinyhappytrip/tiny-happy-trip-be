<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tinyhappytrip.playlist.mapper.PlaylistCommentMapper">
    <insert id="insert">
        INSERT INTO playlist_comments(user_id, playlist_id, content)
        VALUES(#{userId}, #{playlistId}, #{content})
    </insert>

    <delete id="delete">
        DELETE
        FROM playlist_comments
        WHERE playlist_comment_id=#{playlistCommentId}
            AND user_id=#{userId}
    </delete>

    <select id="selectCommentByPlaylistId">
        SELECT playlist_comment_id, user_id, playlist_id, content, created_at
        FROM playlist_comments
        WHERE playlist_id=#{playlistId}
    </select>
</mapper>
