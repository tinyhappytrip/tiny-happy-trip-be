<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tinyhappytrip.playlist.mapper.PlaylistLikeMapper">

    <select id="selectCountByPlaylistIdAndUserId">
        SELECT COUNT(*)
        FROM playlist_likes
        WHERE user_id=#{userId} AND playlist_id=#{playlistId}

    </select>
    <insert id="insert">
        INSERT INTO playlist_likes(user_id, playlist_id)
        VALUES (#{userId}, #{playlistId})
    </insert>

    <delete id="delete">
        DELETE
        FROM playlist_likes
        WHERE user_id=#{userId} AND playlist_id=#{playlistId}
    </delete>

    <select id="selectCountByPlaylistId">
        SELECT COUNT(*)
        FROM playlist_likes
        WHERE playlist_id=#{playlistId}
    </select>

    <select id="selectUserLikePlaylist">
        SELECT playlist_id
        FROM playlist_likes
        WHERE user_id=#{userId}
    </select>

    <select id="selectTopThreePlaylist">
        SELECT playlist_id
        FROM playlist_likes
        GROUP BY playlist_id
        ORDER BY count(*) DESC
        LIMIT 3
    </select>

</mapper>